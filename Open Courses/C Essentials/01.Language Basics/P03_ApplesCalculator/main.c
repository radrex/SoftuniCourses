#include <stdio.h>
#include <stdlib.h>

int main()
{
    int first_tree = 150;
    int second_tree = 142;
    int third_tree = 127;

    int total_apples = first_tree + second_tree + third_tree;
    printf("Apples gathered = %d", total_apples);

    return 0;
}
