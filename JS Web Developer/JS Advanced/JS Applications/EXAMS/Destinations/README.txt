Step 1: Run "npm install" to install dependencies.
Step 2: I'm using lite-server. To run the app use "npm start" command.

When navigating, please click on the text in the button. CSS is written that way <li><a></a></li>.
And the styles are written for <li> not <a>.

Note: Added bs-config.json for multiple browsers. I'm using Firefox.
Note: I have some problems with using hash in urls with Sammy.
      So, I don't use # in url's. If you have problems with that,
      feel free to add # in the paths in .html files.
      Sorry for the inconvenience.